export const connection: Connection = {
  CONNECTION_STRING: 'CONNECTION_STRING',
  DB: 'postgres',
  DBNAME: 'nest-boilerplate',
};
export type Connection = {
  CONNECTION_STRING: string;
  DB: string;
  DBNAME: string;
};
